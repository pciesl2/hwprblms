#include <iostream>

using namespace std;

struct NODE 
{
  int val;
  NODE *next;
};

bool has_dups(NODE *lst) //pass in head ptr
{
    int * n;
    int * i;
    while(i->next != null)
    {
        n = 1st;
        i = n->next;
        if(n-> val == i-> val)
        {
            return true;
        }
    }
}
